fn main() {     
    {                       // s is not valid here, not declared
        let s = "hello";    // s is valid from this point ownard
    }                       // s is no longer valid
}
